services:
  - type: web
    name: scheduling-software
    env: python
    buildCommand: pip install -r requirements.txt || pip install requests gunicorn
    startCommand: python server.py
    autoDeploy: true
